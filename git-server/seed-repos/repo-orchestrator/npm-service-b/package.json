{
  "name": "npm-service-b",
  "version": "1.0.0",
  "description": "test project for an npm project B",
  "main": "index.js",
  "scripts": {
    "wait": "bash -c 'for i in {1..10}; do echo \"Project B: $i\"; sleep 1; done'",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
